# Inputs

Protocol: `lore-maintainer-context/v1`. It includes the task, repository revision, selected records, selection reasons, evidence, validation commands, skill path, output schema path, and proposal destination.
